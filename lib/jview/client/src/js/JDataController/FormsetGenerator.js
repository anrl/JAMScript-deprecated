export function transformFormsetData (formList, actionList) {
    return {
        formList: formList,
        actionList: actionList,
        className: ' formset2 whiteBgColor',
        columns: 3
    }
}